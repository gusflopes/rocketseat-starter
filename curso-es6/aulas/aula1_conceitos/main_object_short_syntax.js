// Executar "yarn dev" no git antes de começar;

// Sintaxe curta de objeto

const nome = 'Gustavo';
const idade = 33;

const usuario = {
    nome,
    idade,
    empresa: 'LSCONT',
};

console.log(usuario);


/* Ao invés de escrever assim, pode escerever como acima
const usuario = {
    nome: nome,
    idade: idade,
    empresa: 'LSCONT',
};
*/
